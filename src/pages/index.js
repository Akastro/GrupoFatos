import * as React from 'react';
import {Button} from '@material-ui/core';

export default function Home(){
  return (
    <div>
      Grupo fatos
      <Button color="primary">Bem vindo</Button>
    </div>
  )
}
